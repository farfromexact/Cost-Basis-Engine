# TODO

## Current Goal

Make the app materially more useful as a professional intraday cost-basis research and decision-support tool, while preserving strict research hygiene: no profitability claims without out-of-sample evidence, no future leakage, explicit A-share sellability constraints, fee/slippage accounting, and baseline comparisons.

## Done
- Isolated dashboard research-review panels so a `Scenario evaluation` failure no longer prevents `Locked-OOS threshold experiments`, `Model-change audit`, or `Audit baseline update review` from rendering; validation: `python -m py_compile app/dashboard.py tests/test_dashboard_evaluation.py`, `python -m pytest tests/test_dashboard_evaluation.py -q --basetemp=pytest_tmp_eval_resilience -o cache_dir=pytest_cache_eval_resilience` (7 passed), and `python -m app.cli evaluate --locked-oos --target-qty 151400 --settled-sellable-qty 151400 --purchasable-qty 15100 --trade-qty 15100` completed all five locked OOS rows.
- Fixed Streamlit Cloud locked-OOS hash mismatch: dataset lock hashes now use canonical LF-normalized CSV content, `datasets/oos/*.csv` is pinned to LF via `.gitattributes`, and cloud-style locked-OOS evaluation no longer fails on Windows-vs-Linux line endings; validation: `python -m py_compile research/dataset_registry.py research/evaluation_report.py app/dashboard.py tests/test_dataset_registry.py tests/test_evaluation_report.py`, `python -m pytest tests/test_dataset_registry.py tests/test_evaluation_report.py tests/test_dashboard_evaluation.py -q --basetemp=pytest_tmp_dataset_hash_fix -o cache_dir=pytest_cache_dataset_hash_fix` (14 passed), and `python -m app.cli evaluate --locked-oos --target-qty 151400 --settled-sellable-qty 151400 --purchasable-qty 15100 --trade-qty 15100` completed all five locked OOS rows.
- Fixed Streamlit Cloud dashboard import failure: `app/dashboard.py` now bootstraps the repository root onto `sys.path` before importing project packages, `.gitignore` excludes local pytest temp/cache folders, and `tests/test_dashboard_deployment_import.py` covers the cloud-style `app/` entrypoint import; validation: `python -m py_compile app/dashboard.py tests/test_dashboard_deployment_import.py`, deployment import smoke `python -c "import os, sys; os.chdir('app'); sys.path=[os.getcwd()]+[p for p in sys.path if p != '']; import dashboard; print('dashboard import ok')"`, focused `python -m pytest tests/test_dashboard_deployment_import.py tests/test_dashboard_evaluation.py -q --basetemp=.runtime\\pytest-tmp-deploy-import -o cache_dir=.runtime\\pytest-cache-deploy-import` (7 passed); full suite attempt was blocked by local Windows pytest tmp-root `PermissionError` in `tmp_path` setup, not by application assertions.
- Fixed dashboard scenario evaluation render crash: `_render_evaluation_report` now accepts the `risk_limit_preset_id` keyword that `main()` passes and that evaluation sizing already uses; validation: `python -m py_compile app/dashboard.py tests/test_dashboard_evaluation.py`, `python -m pytest tests/test_dashboard_evaluation.py tests/test_dashboard_risk_limits.py -q --basetemp=.runtime\\pytest-tmp-dashboard-eval-fix -o cache_dir=.runtime\\pytest-cache-dashboard-eval-fix` (9 passed), and full `python -m pytest -q --basetemp=.runtime\\pytest-tmp-full-dashboard-eval-fix -o cache_dir=.runtime\\pytest-cache-full-dashboard-eval-fix` (191 passed).
- Fixed dashboard execution journal history rendering crash: `_render_execution_journal_panel` now calls the imported `build_execution_journal_history_table` helper instead of an undefined private name; validation: `python -m py_compile app/dashboard.py tests/test_dashboard_execution_journal.py`, `python -m pytest tests/test_dashboard_execution_journal.py tests/test_execution_journal.py -q --basetemp=.runtime\\pytest-tmp-dashboard-fix -o cache_dir=.runtime\\pytest-cache-dashboard-fix` (7 passed), and full `python -m pytest -q --basetemp=.runtime\\pytest-tmp-full-after-dashboard-fix -o cache_dir=.runtime\\pytest-cache-full-after-dashboard-fix` (190 passed).
- Added explicit closeout signoff export for reviewed EOD snapshots: `app.closeout_signoff` now previews signoff eligibility, CLI `trigger` writes a JSON snapshot only with `APPROVE_EOD_CLOSEOUT_SIGNOFF` when closeout is countable or no-action, and dashboard shows preview-only export status; validation: `python -m py_compile app/closeout_signoff.py app/cli.py app/dashboard.py tests/test_closeout_signoff.py tests/test_dashboard_closeout_signoff.py tests/test_cli.py`, focused `python -m pytest tests/test_closeout_signoff.py tests/test_dashboard_closeout_signoff.py tests/test_cli.py tests/test_session_closeout.py tests/test_end_of_day_review.py -q --basetemp=.runtime\\pytest-tmp-focused -o cache_dir=.runtime\\pytest-cache-focused` (22 passed), full `python -m pytest -q --basetemp=.runtime\\pytest-tmp-full -o cache_dir=.runtime\\pytest-cache-full` (190 passed), and CLI `trigger` smoke wrote `.runtime\\closeout_signoff_smoke\\eod-signoff-scenario-mean_revert-2026-01-02.json`.
- Added per-pair closeout attribution: `session_closeout` now lists each pair with buy/sell quantities, fill count, broker matched count, net cash after fees/slippage, countable flag, and blocking reason; dashboard closeout panel shows the pair attribution table; validation: `python -m py_compile app/session_closeout.py app/dashboard.py tests/test_session_closeout.py tests/test_dashboard_session_closeout.py`, `python -m pytest tests/test_session_closeout.py tests/test_dashboard_session_closeout.py --basetemp .runtime\\pytest-tmp -q` (7 passed, 2 cache warnings), `python -m pytest --basetemp .runtime\\pytest-tmp` (184 passed, 2 cache warnings), and CLI `trigger` smoke confirmed `session_closeout.pair_attributions` is present.
- Added compact end-of-day dashboard/CLI review view comparing current `session_closeout` with recent persisted execution journals; CLI `trigger` now emits `end_of_day_review`, and dashboard shows a compact review panel; validation: `python -m py_compile app/end_of_day_review.py app/cli.py app/dashboard.py tests/test_end_of_day_review.py tests/test_dashboard_end_of_day_review.py tests/test_cli.py`, `python -m pytest tests/test_end_of_day_review.py tests/test_dashboard_end_of_day_review.py tests/test_cli.py --basetemp .runtime\\pytest-tmp -q` (12 passed, 2 cache warnings), `python -m pytest --basetemp .runtime\\pytest-tmp` (182 passed, 2 cache warnings), and CLI `trigger` smoke confirmed `end_of_day_review.status=NO_ACTION` with one recent persisted journal.
- Added end-of-day session closeout checks that gate countable cost-basis reduction on closed manual pairs, full broker reconciliation, restored inventory, and no open risk-limit breach; CLI `trigger` and dashboard now expose `session_closeout`; validation: `python -m py_compile app/session_closeout.py app/cli.py app/dashboard.py tests/test_session_closeout.py tests/test_dashboard_session_closeout.py tests/test_cli.py`, `python -m pytest tests/test_session_closeout.py tests/test_dashboard_session_closeout.py tests/test_cli.py --basetemp .runtime\\pytest-tmp -q` (12 passed, 2 cache warnings), `python -m pytest --basetemp .runtime\\pytest-tmp` (177 passed, 2 cache warnings), and CLI `trigger` smoke confirmed `session_closeout.status=NO_ACTION`, `countable=false`, and zero countable reduction when no manual fills exist.
- Persisted session execution journals under `.runtime/execution_journals`: CLI `trigger` and dashboard now save each journal snapshot, expose the saved path, and list recent records for end-of-day review across sessions; validation: `python -m py_compile app/execution_journal.py app/cli.py app/dashboard.py tests/test_execution_journal.py tests/test_dashboard_execution_journal.py`, `python -m pytest tests/test_execution_journal.py tests/test_dashboard_execution_journal.py --basetemp .runtime\\pytest-tmp -q` (7 passed, 2 cache warnings), `python -m pytest --basetemp .runtime\\pytest-tmp` (171 passed, 2 cache warnings), and `python -m app.cli trigger --scenario mean_revert --held-qty 10000 --settled-sellable-qty 10000 --purchasable-qty 10000 --max-t-ratio 0.10 --risk-preset defensive --ignore-fees` confirmed `execution_journal_path` plus `recent_execution_journals` output.
- Added explicit reviewed broker-fill promotion workflow: broker-only reconciliation rows can now be previewed for manual-fill promotion only after pair assignment and the exact review token `APPROVE_BROKER_FILL_PROMOTION`; CLI/dashboard helpers expose the review checks without auto-importing broker rows; validation: `python -m py_compile app/broker_import.py app/cli.py app/dashboard.py tests/test_broker_import.py tests/test_cli_broker_import.py tests/test_dashboard_broker_promotion.py`, `python -m pytest tests/test_broker_import.py tests/test_cli_broker_import.py tests/test_dashboard_broker_promotion.py --basetemp .runtime\\pytest-tmp -q` (13 passed, 2 cache warnings), `python -m pytest --basetemp .runtime\\pytest-tmp` (168 passed, 2 cache warnings), and `python -m app.cli broker-promote` smoke confirmed preview-only without token and write-after-review with token.
- Added a session-level execution journal linking signal, pre-trade ticket, manual fills, broker reconciliation, post-trade review, and live risk usage into one audit report; CLI trigger and dashboard now expose the journal without routing orders or inferring fills; validation: `python -m py_compile app/execution_journal.py app/cli.py app/dashboard.py tests/test_execution_journal.py tests/test_dashboard_execution_journal.py`, `python -m pytest --basetemp .runtime\\pytest-tmp` (161 passed, 2 cache warnings), and `python -m app.cli trigger --scenario mean_revert --held-qty 10000 --settled-sellable-qty 10000 --purchasable-qty 10000 --max-t-ratio 0.10 --risk-preset defensive --ignore-fees` confirmed `execution_journal` output.
- Added broker-confirmed order/fill import adapter scaffolding: CSV/JSON broker fill exports can now be parsed and reconciled against manual fills by exact symbol/side/qty/price/timestamp key; CLI/dashboard surface matched, broker-only, manual-only, and ambiguous rows without auto-importing; validation: `python -m py_compile app/broker_import.py app/cli.py app/dashboard.py tests/test_broker_import.py tests/test_cli_broker_import.py tests/test_dashboard_broker_import.py`, `python -m pytest` (157 passed), and `python -m app.cli broker-import --path .runtime\\broker_import_smoke\\broker.csv --manual-fills-path .runtime\\broker_import_smoke\\manual_fills.json --symbol 603236` confirmed `status=OK` and `matched_count=1`.
- Added live-session risk usage tracking from manual broker fills against the selected risk-limit preset; CLI/dashboard now show session turnover, unclosed pair capital at risk, and open-pair age versus preset limits; validation: `python -m py_compile app/session_risk.py app/cli.py app/dashboard.py tests/test_session_risk.py tests/test_dashboard_session_risk.py`, `python -m pytest` (150 passed), and `python -m app.cli trigger --scenario mean_revert --held-qty 10000 --settled-sellable-qty 10000 --purchasable-qty 10000 --max-t-ratio 0.10 --risk-preset defensive --ignore-fees` confirmed `live_session_risk_usage` output.
- Added post-trade review report comparing manual broker fills with the pre-trade order ticket and execution sensitivity bands; dashboard and CLI trigger output now show whether fills match side/qty/price/cost/sensitivity assumptions; validation: `python -m py_compile app/post_trade_review.py app/cli.py app/dashboard.py tests/test_post_trade_review.py tests/test_dashboard_post_trade_review.py`, `python -m pytest` (145 passed), and `python -m app.cli trigger --scenario mean_revert --held-qty 10000 --settled-sellable-qty 10000 --purchasable-qty 10000 --max-t-ratio 0.10 --risk-preset defensive --ignore-fees` confirmed `post_trade_review` output.
- Added explicit professional intraday risk-limit presets (`defensive`, `balanced`, `active`) covering max round-trip turnover, max open-pair minutes, and max same-day capital at risk; presets now cap trigger sizing and are exposed in CLI/dashboard position state; validation: `python -m py_compile research/risk_limits.py research/trigger_engine.py app/position_state.py app/cli.py app/dashboard.py tests/test_risk_limits.py tests/test_trigger_engine_risk_limits.py tests/test_dashboard_risk_limits.py tests/test_cli.py`, `python -m pytest` (139 passed), and `python -m app.cli trigger --scenario mean_revert --held-qty 10000 --settled-sellable-qty 10000 --purchasable-qty 10000 --max-t-ratio 0.10 --risk-preset defensive --ignore-fees`.
- Added dashboard locked-OOS threshold experiment comparison tables so aggregate and per-scenario signal deltas are visible without reading raw JSON; validation: `python -m py_compile app/dashboard.py tests/test_dashboard_threshold_experiments.py` and `python -m pytest` (133 passed).
- Added explicit audit baseline update workflow for CLI and dashboard: baseline writes require audit deltas plus the exact review token `APPROVE_LOCKED_OOS_BASELINE_UPDATE`; validation: `python -m py_compile research/model_audit.py app/cli.py app/dashboard.py tests/test_model_audit_baseline_update.py tests/test_cli_baseline_update.py tests/test_dashboard_baseline_update.py`, `python -m pytest` (131 passed), and `python -m app.cli audit-baseline-update`.
- Added model threshold experiment runner (`threshold-experiments`) that evaluates built-in threshold variants against locked-OOS audit deltas without modifying the stored baseline; validation: `python -m py_compile research/threshold_experiments.py app/cli.py tests/test_threshold_experiments.py tests/test_cli_threshold_experiments.py`, `python -m pytest` (125 passed), and `python -m app.cli threshold-experiments --experiments more_selective`.
- Added reproducible locked OOS capture CLI (capture-oos) that writes normalized minute CSVs, verifies SHA-256, emits a registry snippet for manual review, and supports csv/eastmoney/yahoo sources; validation: python -m py_compile research/oos_capture.py app/cli.py tests/test_oos_capture.py tests/test_cli_oos_capture.py, python -m pytest (121 passed), and python -m app.cli capture-oos --source csv --symbol 000001 --date 20260612 --csv datasets/oos/000001_20260612_yahoo_intraday.csv --output-dir .runtime/<capture_oos_smoke> --min-bars 300.
- Added model-change audit report comparing current trigger thresholds and locked OOS signal metrics against `research/baselines/locked_oos_audit_baseline_v1.json`; validation: `python -m py_compile research/model_audit.py app/cli.py app/dashboard.py tests/test_model_audit.py tests/test_dashboard_model_audit.py`, `python -m pytest` (116 passed), and `python -m app.cli audit`.
- Added four additional independent locked OOS datasets across symbols/dates, bringing locked OOS coverage to 5 public-feed rows across 3 symbols and 4 dates; validation: `python -m py_compile research/dataset_registry.py research/evaluation_report.py app/dashboard.py tests/test_dataset_registry.py tests/test_evaluation_report.py tests/test_dashboard_evaluation.py`, `python -m pytest` (111 passed), and `python -m app.cli evaluate --locked-oos --target-qty 151400 --settled-sellable-qty 151400 --purchasable-qty 15100 --trade-qty 15100`.
- Added execution-quality/slippage sensitivity bands so actionable guidance shows whether estimated edge survives worse fills; validation: `python -m py_compile app/execution_sensitivity.py app/cli.py app/dashboard.py tests/test_execution_sensitivity.py tests/test_dashboard_execution_sensitivity.py` and `python -m pytest` (110 passed).
- Added a real locked OOS Eastmoney CSV dataset for 000001 on 2026-06-18, SHA-256 validation, `evaluate --locked-oos`, and dashboard OOS table fields; validation: `python -m py_compile research/dataset_registry.py research/evaluation_report.py app/cli.py app/dashboard.py tests/test_dataset_registry.py tests/test_evaluation_report.py tests/test_dashboard_evaluation.py`, `python -m pytest` (106 passed), and `python -m app.cli evaluate --locked-oos --target-qty 151400 --settled-sellable-qty 151400 --purchasable-qty 15100 --trade-qty 15100`.
- Added pre-trade order ticket checklist comparing suggested order side/qty/price against broker snapshot capacity, cash, price-limit risk, and fee/slippage estimates; validation: `python -m py_compile app/order_ticket.py app/cli.py app/dashboard.py tests/test_order_ticket.py tests/test_dashboard_order_ticket.py` and `python -m pytest` (103 passed).
- Added manual broker position reconciliation workflow so persisted state can be checked against broker/account snapshots; validation: `python -m py_compile app/position_reconciliation.py app/cli.py app/dashboard.py tests/test_position_reconciliation.py tests/test_cli_reconciliation.py tests/test_dashboard_reconciliation.py` and `python -m pytest` (96 passed).
- Added data-source delay/licensing caveat panel so live guidance distinguishes research feeds from broker-confirmed market data; validation: `python -m py_compile research/source_disclosure.py app/dashboard.py tests/test_source_disclosure.py` and `python -m pytest` (88 passed).
- Added open-pair execution checklist and manual fill recorder so close/restore actions are tracked from manual broker fills only; validation: `python -m py_compile app/manual_fills.py app/cli.py app/dashboard.py tests/test_manual_fills.py tests/test_cli_manual_fills.py` and `python -m pytest` (85 passed).

- Created the project iteration tracker.
- Restored the dashboard entrypoint to a testable ASCII-only baseline.
- Kept chart signal markers off by default so dense SB/BS annotations do not obscure price/VWAP structure.
- Restored signal marker cooldown support used by dashboard tests.
- Added a trigger-engine signal quality gate: actionable triggers now require trigger-level deviation strength, liquidity confirmation, and post-cost edge; weak setups remain watch-only.
- Converted broken trigger-engine prompt strings to ASCII to keep Windows validation stable.
- Added a professional decision summary that separates recommendation, evidence, invalidation, position impact, and caveats.
- Added multi-scenario evaluation reporting that compares no-trade, a simple S->B replay baseline, and trigger-engine signal diagnostics.
- Added explicit UI data-quality diagnostics for stale, sparse, and approximate minute data before decision confidence is interpreted.
- Added stateful opportunity lifecycle tracking for chart markers: open, close-ready, expired, and blocked are displayed as signal-only states, not fills or PnL.
- Validation passed: `python -m py_compile research/opportunity_lifecycle.py app/dashboard.py tests/test_opportunity_lifecycle.py tests/test_dashboard_signals.py`; `python -m pytest` -> 62 passed.
- Added persistent position/open-pair state shared by dashboard refreshes and CLI monitoring through `.runtime/position_state.json`.
- Validation passed: `python -m py_compile app/position_state.py app/dashboard.py app/cli.py tests/test_cli.py`; `python -m pytest` -> 65 passed.
- Added a rendered dashboard evaluation table comparing no-trade, simple interpretable baseline, and trigger-engine signal diagnostics across synthetic scenarios.
- Validation passed: `python -m py_compile app/dashboard.py tests/test_dashboard_evaluation.py`; `python -m pytest` -> 67 passed.
- Added broker/user fee profile presets so dashboard and CLI guidance default to costed profiles; zero-fee mode now requires explicit research-only selection.
- Validation passed: `python -m py_compile core/fee_profiles.py app/position_state.py app/cli.py app/dashboard.py tests/test_fee_profiles.py tests/test_cli.py tests/test_dashboard_evaluation.py`; `python -m pytest` -> 76 passed.
- Added an out-of-sample dataset registry and split enforcement so evaluation rows are explicitly labeled `in_sample` or `out_of_sample`; current synthetic fixtures are registered as in-sample only.
- Validation passed: `python -m py_compile research/dataset_registry.py research/evaluation_report.py app/dashboard.py app/cli.py tests/test_dataset_registry.py tests/test_evaluation_report.py tests/test_dashboard_evaluation.py`; `python -m pytest` -> 79 passed.

## Next

1. Add a broker/export freshness check so closeout and EOD review warn when broker files or manual fills are stale versus the session date.
2. Add per-session realized-vs-unrealized ledger summary separating countable closeout reduction, blocked pair net cash, and no-action days.
## Blockers

- Only five public-feed locked OOS rows are registered, so the app must still not claim profitability or production trading validity.














