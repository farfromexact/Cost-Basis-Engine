[project]
name = "cost-basis-engine"
version = "0.1.0"
description = "Local research engine for single-stock A-share intraday cost-basis T strategy replay."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
